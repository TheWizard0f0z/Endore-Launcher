public class ProfileInfo
{
    public string Name { get; set; }      // Nazwa wyświetlana (np. "Moj Wojownik")
    public string FileName { get; set; }  // Nazwa pliku (np. "moj_wojownik.json")
    public string FullPath { get; set; }  // Pełna ścieżka do pliku
}